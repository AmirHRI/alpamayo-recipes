_target_: alpamayo1_sft.models.sft_base_model.TrainableReasoningVLA.from_pretrained_vlm
vlm_name_or_path: /temp/achahe/hf_cache/hub/Cosmos-Reason2-2B
# Read all trajectory tokenizer and image-resolution settings directly from the
# Alpamayo-R1-10B config.json (traj_vocab_size, tokens_per_{history,future}_traj,
# traj_tokenizer_cfg, hist_traj_tokenizer_cfg, min/max_pixels, etc.).
# Works with both a flat local-dir and an HF hub cache directory.
alpamayo_config_path: /temp/achahe/hf_cache/hub/models--nvidia--Alpamayo-R1-10B
# Set by evaluate_hf.py to load fine-tuned VLM weights from a Stage-1 checkpoint.
# Leave null for normal training (from_pretrained_vlm will load from vlm_name_or_path).
checkpoint_path: null
